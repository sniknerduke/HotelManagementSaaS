# ============================================
# PRODUCTION OVERRIDES (npm run build)
# ============================================

# In production, frontend is served from the same domain as the API gateway.
# API calls go directly to https://sniknerduke.dev/api via reverse proxy.
VITE_API_URL=https://sniknerduke.dev
